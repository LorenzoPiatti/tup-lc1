
# OPERADORES COMUNES:
# +                       Operador de concatenación de cadenas de caracteres
# or, not, and            Operadores lógicos o booleanos
# >, >=, <, >=, ==, !=    Operadores de comparación
# +, -, *, /, %, //, **   Operadores aritméticos en Python


# OPERADORES A NIVEL DE BITS:
# x | y               or bit a bit de x e y.
# x ^ y     or exclusivo bit a bit de x e y.
# x & y              and bit a bit de x e y.
# x << n                 Desplaza x n bits a la izquierda.
# x >> n                 Desplaza x n bits a la derecha.
# ~x not x               Obtiene los bits de x invertidos.


# OPERADORES DE ASIGNACIÓN
# Operador    Ejemplo    Equivalencia
#    =         x = 2
#   +=         x += 2      x = x + 2
#   -=         x -= 2      x = x – 2
#   *=         x *= 2      x = x * 2
#   /=         x /= 2      x = x / 2
#   %=         x %= 2      x = x % 2
#   //=        x //= 2     x = x // 2
#   **=        x **= 2     x = x ** 2
#   &=         x &= 2      x = x & 2
#   |=         x |= 2      x = x | 2
#   ^=         x ^= 2      x = x ^ 2
#   >>=        x >>= 2     x = x >> 2
#   <<=        x <<= 2     x = x << 2


# OPERADORES DE PERTENENCIA:
# in            Devuelve True si el valor se encuentra en una secuencia; False en caso contrario.
# not in        Devuelve True si el valor se encuentra en una secuencia; False en caso contrario.


# OPERADORES DE IDENTIDAD:
# is            Devuelve True si ambas variables son el mismo objeto.
# is not        Devuelve False si ambas variables no son el mismo objeto.